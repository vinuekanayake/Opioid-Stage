model_name: "t5-3b"
model_type: "encoder-decoder"
batch_size: 1
learning_rate: 1e-4
epochs: 8  # Summarized reasoning epochs
max_length: 512
gradient_accumulation_steps: 8
prefix: "classify:"
max_generation_length: 512  # For reasoning outputs
use_reasoning: true
